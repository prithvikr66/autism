
const TokenInfo = () => {
  //   const { ca } = useParams();
  return (
    <div className=" mt-[300px] w-full flex justify-center items-center font-sofia-regular uppercase text-[40px] text-black">
      work in progress
    </div>
  );
};

export default TokenInfo;
